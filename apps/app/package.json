{
    "name": "@monorepo/app",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "test": "echo test app && exit 0",
        "lint": "eslint **/*.ts",
        "tsc": "tsc --noEmit",
        "build": "echo build app && exit 0"
    },
    "dependencies": {
        "@monorepo/c": "*",
        "@monorepo/a": "*",
        "@monorepo/b": "*"
    },
    "devDependencies": {
        "typescript": "^4.6.4"
    }
}
